import Foundation

/// The scope of translation in the Reference Pane.
enum TranslationMode {
    /// Translate only the selected text.
    case selection

    /// Translate the entire page content.
    case fullPage
}
